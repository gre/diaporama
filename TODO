Video
=====

- Making video in sync with currentTime
- use "image" field to define a fallback
- ? unify / share the kenburns part

- ensure unsupported video fallback nicely (black)
- Check everything works everywhere (mobile)
- update documentation
